export type TimeOfDay = {
  hour: string;
  minute: string;
};

export const validateTimeOfDay = (tod: TimeOfDay): boolean => {
  // use RegExp
  throw new Error('validateTimeOfDay: not yet implemented');
};
